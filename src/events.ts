export enum Events {
  UserConnect = 'user.connect',
  UserUpdate = 'user.update',
  UserDisconnect = 'user.disconnect',
  ServerUserUpdate = 'server.user.update',
  ServerPlayersUpdate = 'server.players.update',
}
